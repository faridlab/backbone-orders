//! OrderDelivery seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial OrderDelivery data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ORDERDELIVERY SEEDER
// ============================================================================

/// Seeder for order_delivery
pub struct SeedOrderDeliverySeeder;

impl SeedOrderDeliverySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedOrderDeliverySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedOrderDeliverySeeder {
    fn name(&self) -> &'static str {
        "SeedOrderDeliverySeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if order_deliveries table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM order_deliveries")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/order_delivery_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM order_deliveries")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

