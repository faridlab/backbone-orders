//! Orders Module Handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - AppState dependency injection container
//! - Custom handler integration points

use std::sync::Arc;

// Import all services
use crate::application::service::DeliveryTierService;
use crate::application::service::OrderService;
use crate::application::service::OrderDeliveryService;
use crate::application::service::OrderItemService;
use crate::application::service::OrderPickupService;
use crate::application::service::OrderStatusTimelineService;

/// Application state for dependency injection.
///
/// This struct holds references to all module services
/// and is passed to HTTP handlers via Axum's State extractor.
///
/// # Example
///
/// ```ignore
/// use axum::extract::State;
///
/// async fn custom_handler(
///     State(state): State<AppState>,
/// ) -> impl IntoResponse {
///     // Access services via state
/// }
/// ```
#[derive(Clone)]
pub struct AppState {
    /// DeliveryTier service
    pub delivery_tier_service: Arc<DeliveryTierService>,
    /// Order service
    pub order_service: Arc<OrderService>,
    /// OrderDelivery service
    pub order_delivery_service: Arc<OrderDeliveryService>,
    /// OrderItem service
    pub order_item_service: Arc<OrderItemService>,
    /// OrderPickup service
    pub order_pickup_service: Arc<OrderPickupService>,
    /// OrderStatusTimeline service
    pub order_status_timeline_service: Arc<OrderStatusTimelineService>,
}

impl AppState {
    /// Create a new AppState with all services.
    pub fn new(
        delivery_tier_service: Arc<DeliveryTierService>,
        order_service: Arc<OrderService>,
        order_delivery_service: Arc<OrderDeliveryService>,
        order_item_service: Arc<OrderItemService>,
        order_pickup_service: Arc<OrderPickupService>,
        order_status_timeline_service: Arc<OrderStatusTimelineService>
    ) -> Self {
        Self {
            delivery_tier_service,
            order_service,
            order_delivery_service,
            order_item_service,
            order_pickup_service,
            order_status_timeline_service,
        }
    }

    /// Create AppState from module instance.
    pub fn from_module(module: &crate::OrdersModule) -> Self {
        Self {
            delivery_tier_service: module.delivery_tier_service.clone(),
            order_service: module.order_service.clone(),
            order_delivery_service: module.order_delivery_service.clone(),
            order_item_service: module.order_item_service.clone(),
            order_pickup_service: module.order_pickup_service.clone(),
            order_status_timeline_service: module.order_status_timeline_service.clone(),
        }
    }
}

/// Builder for AppState.
///
/// Allows incremental construction of AppState.
#[derive(Default)]
pub struct AppStateBuilder {
    delivery_tier_service: Option<Arc<DeliveryTierService>>,
    order_service: Option<Arc<OrderService>>,
    order_delivery_service: Option<Arc<OrderDeliveryService>>,
    order_item_service: Option<Arc<OrderItemService>>,
    order_pickup_service: Option<Arc<OrderPickupService>>,
    order_status_timeline_service: Option<Arc<OrderStatusTimelineService>>,
}

impl AppStateBuilder {
    /// Create a new builder.
    pub fn new() -> Self {
        Self::default()
    }

    /// Set the DeliveryTier service.
    pub fn with_delivery_tier_service(mut self, service: Arc<DeliveryTierService>) -> Self {
        self.delivery_tier_service = Some(service);
        self
    }

    /// Set the Order service.
    pub fn with_order_service(mut self, service: Arc<OrderService>) -> Self {
        self.order_service = Some(service);
        self
    }

    /// Set the OrderDelivery service.
    pub fn with_order_delivery_service(mut self, service: Arc<OrderDeliveryService>) -> Self {
        self.order_delivery_service = Some(service);
        self
    }

    /// Set the OrderItem service.
    pub fn with_order_item_service(mut self, service: Arc<OrderItemService>) -> Self {
        self.order_item_service = Some(service);
        self
    }

    /// Set the OrderPickup service.
    pub fn with_order_pickup_service(mut self, service: Arc<OrderPickupService>) -> Self {
        self.order_pickup_service = Some(service);
        self
    }

    /// Set the OrderStatusTimeline service.
    pub fn with_order_status_timeline_service(mut self, service: Arc<OrderStatusTimelineService>) -> Self {
        self.order_status_timeline_service = Some(service);
        self
    }

    /// Build the AppState.
    ///
    /// # Panics
    ///
    /// Panics if any required service is not set.
    pub fn build(self) -> AppState {
        AppState {
            delivery_tier_service: self.delivery_tier_service.expect("delivery_tier_service is required"),
            order_service: self.order_service.expect("order_service is required"),
            order_delivery_service: self.order_delivery_service.expect("order_delivery_service is required"),
            order_item_service: self.order_item_service.expect("order_item_service is required"),
            order_pickup_service: self.order_pickup_service.expect("order_pickup_service is required"),
            order_status_timeline_service: self.order_status_timeline_service.expect("order_status_timeline_service is required"),
        }
    }
}
